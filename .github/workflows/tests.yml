name: Tests

on:
  push:
    branches: [ "main", "develop" ]
  pull_request:
    branches: [ "main", "develop" ]
  workflow_dispatch:

# Declare default permissions as read only
permissions: read-all

jobs:
  php-unit-tests:
    name: PHP Unit Tests
    runs-on: ubuntu-latest
    permissions:
      contents: read
    
    strategy:
      matrix:
        php-version: ['7.4', '8.0', '8.1', '8.2', '8.3']
        wordpress-version: ['6.0', '6.1', '6.2', '6.3', '6.4']
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v5
    
    - name: Setup PHP ${{ matrix.php-version }}
      uses: shivammathur/setup-php@v2
      with:
        php-version: ${{ matrix.php-version }}
        extensions: mysqli
        coverage: none
    
    - name: Setup WordPress Test Environment
      run: |
        echo "WordPress test environment would be configured here"
        echo "This is a placeholder for future test implementation"
    
    - name: Run Tests
      run: |
        echo "Tests would run here"
        echo "Currently no unit tests implemented - plugin uses manual testing"
        echo "See TESTING.md for manual testing procedures"
